import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rwaactivated.passport',
  appName: 'RWA Passport',
  webDir: 'dist-ios'
};

export default config;
